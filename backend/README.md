# Nrityasana API

Spring Boot 3.5 REST service for the Nrityasana Flutter client.

## Endpoints

- `GET /api/practices` returns the starter practice catalog.

Run with `mvn spring-boot:run` from this directory.
